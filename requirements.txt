# Core dependencies
flask>=2.0.0
flask-cors>=3.0.0
pyyaml>=6.0
connexion>=3.0.0
jsonschema>=4.0.0

# Development dependencies (install with: pip install -r requirements.txt[dev])
pytest>=6.0
pytest-cov
black
flake8
mypy
